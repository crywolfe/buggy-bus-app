describe "the search" do

  # describe "a user sees the search results" do

    # it "says search params" do
    #   visit("/searches/:id")
    #   expect( page ).to have_content "search[params]"
    # end

    # it "should list trips for a given origin, destination and date" do
    #    origin = "NYC"
    #    destination = "Boston"
    #    date = "5/5/1994"
    #    expect(Search.new(departure_location: arrival_location: departure_date:).length).to eq(7)
    # end
  end
